// src/Components/Seller/EditProduct/ErrorMessage.jsx
import React from 'react';

const ErrorMessage = ({ message }) => {
  return (
    <p className="error-message">{message}</p>
  );
};

export default ErrorMessage;
